# 变更记录

## 0.3.2 - 2026-08-10

### 修复

- 列表和实例信息按数据目录对应的 yasdb 进程显示实时状态。
- 状态统一为 `FAIL`、`INSTALLED`、`RUNNING`、`NOT RUN`。

## 0.3.1 - 2026-08-10

### 修复

- MySQL 地址只更新 `[[group.node]]` 中 yasboot 生成的 `mysql_addr`。

## 0.3.0 - 2026-08-10

### 新增

- 支持 `--mysql` 自动分配 MySQL 端口，以及 `--mysql-port PORT` 显式指定端口。
- 列表、实例信息和环境变量显示 MySQL 模式及端口。

### 变更

- 删除 `--recommend-memory` 和 `MEMORY_LIMIT`，只保留绝对内存配置。

## 0.2.4 - 2026-08-10

### 修复

- 组合发布包排除 myas 和 yinstall 的 `logs`、`dist` 运行产物。

## 0.2.3 - 2026-08-10

### 修复

- 兼容默认非推荐模式生成的无 `memory_limit` TOML，绝对内存配置会插入缺失字段。

## 0.2.2 - 2026-08-10

### 变更

- 默认采用生成后的 TOML 内存配置，新增 `--recommend-memory` 显式启用推荐内存。
- `--memory-size SIZE` 继续支持按 M/G 绝对值覆盖本次部署配置。

## 0.2.1 - 2026-08-10

### 修复

- 绝对内存参数直接写入 hosts 和集群 TOML，不再受整数百分比粒度限制。

## 0.2.0 - 2026-08-10

### 安全

- 移除仓库和公开发布包中的默认密码，改为部署时注入占位符（MYAS-008）。
- 从 myas 仓库移除 yinstall gitlink，改由两个独立仓库组装发布（MYAS-009）。

## 0.1.15 - 2026-08-10

### 修复

- shell-init 和实例别名使用稳定入口，避免升级后当前终端继续调用旧版本（MYAS-007）。

## 0.1.14 - 2026-08-10

### 修复

- `myas env` 加载 yasboot 生成的实例环境，修复 yasql SYSDBA 本地连接（MYAS-006）。

## 0.1.13 - 2026-08-10

### 修复

- 内置 yinstall 验证步骤不再由非 root 用户调用 runuser。

## 0.1.12 - 2026-08-10

### 修复

- 修复目标机非交互验证找不到 `runuser`，完成已部署实例状态检查。

## 0.1.11 - 2026-08-10

### 修复

- 自动发布重试时支持 `myas create --force` 清理失败安装残留。

## 0.1.10 - 2026-08-10

### 修复

- 兼容 yasboot 生成的缩进 TOML section，修复 C-005 端口配置失败（MYAS-004）。

## 0.1.9 - 2026-08-10

### 修复

- 修复 yinstall C-001 检查脚本中嵌套双引号导致的 `unexpected end of file`。
- 发布包包含该修复，并记录问题 `MYAS-003`。

## 0.1.8 - 2026-08-07

### 修复

- 使用 `/usr/bin/sudo` 执行本地特权步骤，避免 PATH 中包装程序干扰。

## 0.1.7 - 2026-08-07

### 修复

- 使用绝对路径调用系统 sudo，避免 PATH 和 shell 包装程序干扰。

## 0.1.6 - 2026-08-07

### 修复

- 避免 PATH 中 devtoolset 的 sudo 包装程序覆盖系统 sudo。

## 0.1.5 - 2026-08-07

### 修复

- 绕过同名 shell 函数，确保 yinstall 的 sudo 调用使用真实命令。

## 0.1.4 - 2026-08-07

### 修复

- 修复通过 `~/.local/bin/myas` 软链接启动时无法定位版本和模块的问题。

## 0.1.3 - 2026-08-07

### 变更

- 内置 `yinstall`，支持从配置目录、部署目录或 PATH 自动发现安装入口。
- 重命名 myas 公共模块为 `myas-common.sh`，避免与 yinstall 冲突。

## 0.1.2 - 2026-08-06

### 变更

- 新增全局 `SYS_PASSWORD`，由部署流程注入并统一传递给 `yinstall`。
- 配置显示时掩码 sys 密码，设置文件权限收紧为仅当前用户可读写。

## 0.1.1 - 2026-08-06

### 变更

- `create` 默认本地部署；仅远程部署需要目标主机和数据库密码。
- `--db-port` 改为可选，并从可配置的 `YASOM_PORT_START` 分配四端口组。

## 0.1.0 - 2026-08-06

### 新增

- 实例元数据、环境切换、别名和生命周期命令。
- 通过 `yinstall` 委托安装，并传递明确的目录与 `db-port`。
